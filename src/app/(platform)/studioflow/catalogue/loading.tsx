import { LoadingState } from "@/platform/ui_engine";

export default function CatalogueLoading() { return <LoadingState title="Loading Product Catalogue" description="Retrieving reusable StudioFlow specifications." />; }
