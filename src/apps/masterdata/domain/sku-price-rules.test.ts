import assert from"node:assert/strict";import{describe,it}from"node:test";import{assertCurrency,assertSkuPriceCanClear,assertSkuPriceContext,normalizePriceAmount}from"./pricing-rules";
describe("SkuPrice rules",()=>{it("preserves explicit zero and rejects blank/negative/over-scale",()=>{assert.equal(normalizePriceAmount("0.00"),"0");assert.throws(()=>normalizePriceAmount(""),{code:"PRICE_AMOUNT_INVALID"});assert.throws(()=>normalizePriceAmount("-1"),{code:"PRICE_AMOUNT_NEGATIVE"});assert.throws(()=>normalizePriceAmount("1.234"),{code:"PRICE_AMOUNT_SCALE_INVALID"})});it("requires explicit currency",()=>{assert.doesNotThrow(()=>assertCurrency("IDR"));assert.throws(()=>assertCurrency("idr"),{code:"PRICE_CURRENCY_INVALID"})});it("validates canonical unit, supplier and source",()=>{const sku={status:"DRAFT"as const,deletedAt:null,baseUnitId:"pcs",purchaseUnitId:null,brandId:"b"};assert.doesNotThrow(()=>assertSkuPriceContext({sku,unit:{id:"pcs",deletedAt:null},supplier:null,source:null,unitId:"pcs",supplierId:null,sourceId:null}));assert.throws(()=>assertSkuPriceContext({sku,unit:{id:"box",deletedAt:null},supplier:null,source:null,unitId:"box",supplierId:null,sourceId:null}),{code:"SKU_PRICE_UNIT_MISMATCH"})});it("clears only Draft",()=>{assert.doesNotThrow(()=>assertSkuPriceCanClear("DRAFT"));assert.throws(()=>assertSkuPriceCanClear("ACTIVE"),{code:"SKU_PRICE_CLEAR_DRAFT_ONLY"})});});
