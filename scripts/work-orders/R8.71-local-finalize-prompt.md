# Executor Prompt — R8.71 local finalize (SF-R1)

Repo: `D:\Misc\ProjectsHUB\studioflowrb`. Read `AGENTS.md`, `PLAN.md`, `docs/apps/studioflow/STUDIOFLOW-REWORK-CONTRACT.md` first.
Legacy (`D:\Misc\ProjectsHUB\studioflow`) and its PostgreSQL are read-only / forbidden. Do not print secrets. Local commit only — no push/PR/deploy.

The SF-R1 source files were already written into the working tree (cloud executor). Your job is to finish the cutover locally, verify, and commit.

## 1. Preconditions
- `git status` — HEAD must be `8fc99c0` (R8.70); tag `archive/studioflow-rb-r8.69` must exist. If not, STOP and report.
- Expected: many modified/new files under `src/apps/studioflow`, `src/app/(platform)/studioflow`, `src/platform`, `prisma`, docs.
- A folder `_incoming/` holds the already-built R8.72 bundle. It is NOT part of R8.71: add the line `_incoming/` to `.git/info/exclude` (local only, not `.gitignore`) before anything else, and never stage it.

## 2. Delete old rebuild StudioFlow code (preserved by the archive tag)
`git rm -r` these (skip any that no longer exist):
- `src/apps/studioflow/`: `catalogue.contract.test.ts`, `labels.ts`, `mom-images.ts`, `mom-images.test.ts`, `mom.contract.test.ts`, `requirements.integration.test.ts`, `requirements.ui.test.ts`, `ui-copy.test.ts`
- `src/app/(platform)/studioflow/`: dirs `[id]/`, `catalogue/`, `library/`, `new/`, `clients/new/`, `clients/[id]/`, `settings/phases/`, `settings/requirements/`; files `settings/actions.ts`, `settings/naming-template-form.tsx`
- `scripts/work-orders/STUDIOFLOW-*.md`
Then list any remaining file under `src/apps/studioflow` or `src/app/(platform)/studioflow` that is NOT imported by the new code and fails typecheck because it references removed models/permissions — delete those too and list them in the report.

## 3. Archive superseded docs
`git mv` the 8 StudioFlow contracts carrying the "Superseded" banner (from R8.70) into `docs/archive/studioflow-rb/` (README there already exists). Fix any relative links that break (`docs/README.md` index).

## 4. Prisma (rebuild DBs only)
- Confirm from `.env.kantor` (without printing values) that `DATABASE_URL` database name is `studioflow_rebuild` and the test URL is `studioflow_rebuild_test`. Any other target → STOP.
- `npx prisma generate`
- `npx prisma migrate deploy` against the test DB, then the kantor dev DB. Migration `20260915100000_sf_r1_legacy_rework_cutover` drops and recreates schema `studioflow` (old rebuild StudioFlow data is disposable per owner decision RW-03).
- `npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url <test-db-url> --exit-code` → must report no difference. If it reports drift, fix the **migration SQL** (not the schema intent) and report.

## 5. Checks
`npm test`, `npm run typecheck`, `npm run lint`, `npm run check:boundaries`, `npm run check:legacy-runtime`, `npm run build`. Cloud baseline: 354/354 tests, all green. Fix only regressions caused by step 2–3 deletions; report anything else.

## 6. Commit
`git add -A` (never `.env.kantor` / `.env.rumah`; confirm `_incoming/` is not staged), commit:
`R8.71 | feat(studioflow): rework legacy project backbone (SF-R1)`
Update `PLAN.md` status line to "implemented and committed in R8.71" before committing. Do NOT start a new PLAN.md. After this commit, continue with `_incoming/R8.72-local-finalize-prompt.md` in the same session.

## 7. Report
HEAD hash, deleted/moved file list, migrate deploy + diff results, check results, anything blocked.
