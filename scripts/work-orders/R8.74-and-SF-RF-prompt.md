# Executor Prompt — R8.74 review corrections, then SF-RF acceptance

Repo: `D:\Misc\ProjectsHUB\studioflowrb` (branch `studioflow/contracts`, HEAD should be R8.73).
Read `AGENTS.md` first. Legacy repo/DB forbidden, no secrets in output, local commits only (no push/PR/deploy).

## Part A — R8.74 (code already in the working tree)

The Planner reviewed SF-R1…SF-R3 and wrote fixes into the working tree (26 files, see `CHANGELOG.md` R8.74; no schema change).

1. `git status --short` — expect exactly the R8.74 files (modified) plus the new `src/apps/studioflow/schedule/sync.ts`. Anything else → STOP and report.
2. If the 8 superseded StudioFlow contracts are still in `docs/apps/studioflow/` (`studioflow.md`, `studioflow-project-contract.md`, `studioflow-mom-contract.md`, `studioflow-schedule-contract.md`, `studioflow-ux-spec.md`, `studioflow-work-orders.md`, `studioflow-implementation-plan.md`, `STUDIOFLOW-LEGACY-AUDIT-ROADMAP.md`): `git mv` them to `docs/archive/studioflow-rb/`, fix relative links (grep `docs/`, root `*.md`, `AGENTS.md`), and add one bullet about it under R8.74 "Fixed" in `CHANGELOG.md`.
3. Checks: `npx prisma generate`, `npm test` (cloud baseline 372/372), `npm run typecheck`, `npm run lint`, `npm run check:boundaries`, `npm run check:legacy-runtime`, `npm run build`. `npx prisma migrate diff … --exit-code` with the separate shadow DB must still report no difference (no migration in R8.74).
4. Fix only failures caused by R8.74; report anything else. Commit:
   `R8.74 | fix(studioflow): wave-1 review corrections (SF-R1–SF-R3)`
   Leave `PLAN.md` as is in this commit — SF-RF gets its own plan in Part B.

## Part B — SF-RF parity acceptance (browser)

Write `PLAN.md` for `SF-RF-WAVE1-PARITY-ACCEPTANCE` (template `docs/agent/PLAN-TEMPLATE.md`), then walk every flow below on the kantor rebuild DB at **1440 px and 375 px** with a real owner account and a drafter-only account. Record pass/fail per line in `docs/review.md` (new "SF-RF" section). A defect → add it to `docs/knownbug.md` (do not silently patch large behavior; small obvious UI fixes are fine and go into the SF-RF commit).

**Projects & clients**
- New project with auto naming (and manual naming when auto is off), new client inline, PIC designer/drafter; edit details; priority; hold/resume; archive with reason → read-only everywhere → restore.
- Clients list/detail, rename, archive (blocked while a running project exists), restore.

**Phases & revisions**
- Moodboard: to-do blocks internal submit; tick → submit internal → feedback blocks approval → "Needs changes" → v1.1 with feedback turned into to-dos (originals closed) → submit → approve → send to client → client changes → v2.0 → client approved → locked.
- Layout / 3D / CD run in parallel; Supervision waits for CD; skip (bypass) with reason; reopen with reason (internal → minor, client → major); "Reopen" not offered on a phase that never started; admin revision reset; finish Supervision completes the project.
- Deferred to-do keeps blocking approval; defer, edit, delete activities; assignee on a former member still editable.

**Checklist & Today**
- Template items seeded on new projects; "Apply checklist templates" does not add to locked phases; subtasks (one level), cascade tick, priority, due, labels, assignee, move up/down.
- Today: my projects grouped, filters, saved filter views, quick add; checked items vanish after 7 days (spot check with data).

**MOM**
- New MOM → header → notes (plain vs numbered) → reorder → photo crop/annotate from a phone photo → second photo, swap, remove → text-only section → Print / Save PDF on A4 → delete. Drafter read-only.
- Decide with the owner: "Plain" note has no number (proposal from R8.72).

**Product Schedule**
- Settings: prefix, default category (reserve row), template item (activate/deactivate/delete).
- New project shows template rows; Add item (with product / reserve code only); options: add, edit, set final, delete final (next becomes final), labels never repeat; qty/unit/location save; move up/down; move to category (code changes, old group closes gap); delete item (codes close gap).
- Copy from a past project (needs a second project with a product).
- Import the real Google Sheets CSV (Material and Fixture): new codes added, existing codes updated, bad/ambiguous category rejects the whole file.
- Drafter read-only; archived project read-only; History shows schedule events.

**Wrap-up (only when everything above passes)**
- Remove the temporary legacy redirects in `next.config.ts` (`/projects…`, `/upcoming`, `/settings/studio`, `/settings/clients`).
- Mark SF-R1…SF-RF done in `docs/roadmap.md`, freeze the baseline note in `docs/review.md`, CHANGELOG entry, full checks, commit
  `R8.75 | chore(studioflow): wave-1 parity acceptance (SF-RF)`.
- Report: HEAD, pass/fail table, knownbug entries, owner decisions still open (Plain note, phase accent palette §13.8).
